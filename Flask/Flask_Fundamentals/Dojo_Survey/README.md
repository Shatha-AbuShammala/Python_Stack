# Dojo Survey Assignment - Flask

## Description
A Flask application that accepts form submissions and displays the submitted data on a results page.

## How to Run
1. Install Flask
pip install flask

2. Run the server
python server.py

3. Open your browser:
- localhost:5000/ → Survey Form
- localhost:5000/result → Submitted Info

## Project Structure
Dojo_Survey/
├── server.py
└── templates/
      ├── index.html
      └── resul